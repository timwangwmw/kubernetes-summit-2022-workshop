apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: test-app-placement-1
  namespace: jace-test
  labels:
    app: test-app
spec:
  clusterSelector:
    matchLabels:
      openshift: "4.9"

